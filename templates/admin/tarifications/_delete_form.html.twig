<form method="post" action="{{ path('tarifications_delete', {'id': tarification.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir le supprimer?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tarification.id) }}">
    <button class="btn">Supprimer</button>
</form>
